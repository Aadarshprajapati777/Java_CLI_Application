# Java_CLI_Application
 fork it
 clone it
 run main.java
 use "teacher" as username and "teacher as password for teacher login.
 use "student" as username and "student" as password for student login.
